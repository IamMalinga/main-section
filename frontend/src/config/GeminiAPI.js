const GeminiAPI = {
    getRecommendations: async () => {
        // Placeholder: Replace with actual API call to Gemini API
        return [
            { name: "Recommended Destination 1" },
            { name: "Recommended Destination 2" },
            { name: "Recommended Destination 3" }
        ];
    }
};

export default GeminiAPI;
